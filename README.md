# bsky to witch

A browser extension that automatically redirects `bsky.app` to `witchsky.app`.

## Install

Right now it's not published on the Chrome Web Store or Firefox Add-ons, but you can load it as an unpacked extension in your browser (you'll need to download this repo as ZIP).